{
	"srvcId": "${artifactId}-${version}",
	"nodeId": "localhost",
	"descriptor": {
		"exec": "java -Dport=%p -jar ../mod-audit-filter/target/mod-audit-filter-fat.jar -Dhttp.port=%p embed_postgres=true"
	}
}

